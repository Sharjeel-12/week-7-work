import { BusinessHoursDirective } from './business-hours.directive';

describe('BusinessHoursDirective', () => {
  it('should create an instance', () => {
    const directive = new BusinessHoursDirective();
    expect(directive).toBeTruthy();
  });
});
